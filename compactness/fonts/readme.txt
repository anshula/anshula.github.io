These fonts are currently not in use.

When these custom fonts are paired with MathJaX, the mathjax text appears very small in the Firefox browser.  If adding these back in (uncommenting the relevant lines of custom-style.css) no longer creates this issue, then we could add these fonts back in.